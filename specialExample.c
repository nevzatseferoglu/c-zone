char special(May Anything) //We can discuss whether it is true or not.
{
	{
	char arr[10]="zybran"
	above the code is equal to below one;
	}


	{
	char *ptr;
	ptr=(char*)malloc(10*sizeof(char));
	free(ptr);
	}

	When We create an "char array" in some sort of function ;
	It will occuring a process that is overhead.
}